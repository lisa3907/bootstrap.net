@inherits ColumnBase

<DynamicElement Attributes="@UnknownParameters" TagName="label" class="@classname" style="@Style">
    @ChildContent
</DynamicElement>

@functions {
    string classname
    {
        get
        {
            var check = IsCheck ? "form-check-label" : null;
            var size = GetColumnClass(null);
            var column = size != null ? "col-form-label" : null;
            return $"{check} {column} {size} {Class}";
        }
    }
    [Parameter] public bool IsCheck { get; set; }
    [Parameter] public string Class { get; set; }
    [Parameter] public string Style { get; set; }
    [Parameter] public RenderFragment ChildContent { get; set; }
}